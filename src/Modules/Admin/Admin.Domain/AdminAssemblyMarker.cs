namespace Admin.Domain;

// Admin owns no aggregates: it is an orchestration and projection layer over Identity, Platform, and other modules' integration events. Intentionally empty.
public static class AdminAssemblyMarker
{
}
