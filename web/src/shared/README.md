# shared

Truly reusable frontend primitives live here.

Shared API transport, query helpers, realtime adapters, telemetry, and UI building blocks belong here instead of being reimplemented inside feature folders.
