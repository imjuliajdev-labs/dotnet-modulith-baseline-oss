# Web.UnitTests

The runnable Vitest files live under `web/tests/unit` so they resolve the frontend workspace dependencies directly.

This directory remains the repo-level anchor for the frontend unit-test gate.
